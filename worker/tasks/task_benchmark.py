from worker.tasks import AbstractTask


class TaskBenchmark(AbstractTask):

    # Should benchmark the relative performance, based on an example project. The project should check for both the
    # time duration

    def __init__(self, **kwargs):
        pass
